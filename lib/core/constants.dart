// lib/core/constants.dart

import 'package:flutter/material.dart';

// --- COULEURS OFFICIELLES (Thème Maroc) ---

// Rouge principal (Maroc / CAN)
const Color kMarocRed = Color(0xFFE62F2F);

// Vert secondaire (Drapeau / Accents)
const Color kMarocGreen = Color.fromRGBO(0, 135, 81, 1);

// Fond sombre principal
const Color kMarocDark = Color(0xFF111111);

// Noir pur (pour les ombres ou textes sur fond clair)
const Color kBlack = Color(0xFF000000);

// Blanc pur et Blanc cassé (pour les textes)
const Color kWhite = Color(0xFFFFFFFF);
const Color kLightGrey = Color(0xFFF8F8F8);

// Gris pour les textes secondaires ou bordures
const Color kGrey = Colors.grey;